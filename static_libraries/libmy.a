#!/bin/bash

# Compile the C files into object files
gcc -c *.c

# Create the static library and add the object files to it
ar -rc *.a *.o

# Index the library for faster symbol lookup (optional)
ranlib *.a

# Clean up intermediate object files
rm *.o

